namespace Project11_ErrorHandle;

class Program
{
    static void Main(string[] args)
    {
        
        Console.Clear();
    
     /*try
        {
            int a = int.Parse(Console.ReadLine());
              int b = int.Parse(Console.ReadLine());
        System.Console.WriteLine(a/b);
        }
        catch (FormatException ex)
        {
            Console.WriteLine(ex.Message);
        }
        catch(OverflowException){//tasma hatasini

        }
        catch(DivideByZeroException)
        {
            System.Console.WriteLine("0‘a bolme yapmaya calisitiginiz icin ");
        }
        //OutOfMemoryException --> Bellek hatasi
        //ArithmeticException --> Aritmetik islem hatasi
        */

        


    
    }
}
